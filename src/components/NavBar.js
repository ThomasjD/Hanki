import React from 'react'
import './ViewCard.css'

const navbar = (props) => {
  return (
    <div className= "Navbar">
      <button >Decks
      </button>

      <button>Add
      </button>

      <button>Search
      </button>

      <button> Logout
      </button>
    </div>

  )
}

export default navbar
